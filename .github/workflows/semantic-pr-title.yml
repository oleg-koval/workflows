name: PR Title

# Enforce semantic/conventional PR title format.
# e.g. "feat(auth): add OAuth2 support" or "fix: handle null pointer"
# NOTE: This workflow requires pull_request_target event (with PR context).
# It is not usable via workflow_call on other events.
on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read

jobs:
  semantic-pr-title:
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
